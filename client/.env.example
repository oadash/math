# Пусто = тот же хост, что и страница: dev → прокси Vite на :3000; прод → Express раздаёт API и SPA.
# Нужно только если фронт на другом домене, чем API:
# VITE_API_URL=https://твой-api.up.railway.app
VITE_API_URL=
